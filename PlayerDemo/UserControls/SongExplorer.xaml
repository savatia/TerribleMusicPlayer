<UserControl x:Class="PlayerDemo.UserControls.SongExplorer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlayerDemo.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="{StaticResource DefaultWhiteBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Style="{StaticResource SongExplorerButton}" Content="Albums"/>
            <Button Content="All Songs" Style="{StaticResource SongExplorerButton}" />
            <Button Content="Artists" Style="{StaticResource SongExplorerButton}" />            
        </StackPanel>
        <Grid x:Name="SongExplorerBody" Grid.Row="1" >
  
        </Grid>
    </Grid>
</UserControl>
